interface:
  display_name: "Prereview Pipeline"
  short_description: "Generate staged diff-to-HTML review previews"
  default_prompt: "Use $prereview-pipeline to run prepare-context, author annotation notes from context, and build a static HTML prereview."

policy:
  allow_implicit_invocation: true
